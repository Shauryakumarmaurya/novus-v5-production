name: Deploy to Azure VM

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Deploy to Azure VM via SSH
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.AZURE_VM_IP }}
        username: ${{ secrets.AZURE_VM_USERNAME }}
        key: ${{ secrets.AZURE_VM_SSH_KEY }}
        script: |
          # Find the directory containing docker-compose.yml in the home folder
          APP_DIR=$(find ~ -maxdepth 3 -name "docker-compose.yml" -type f 2>/dev/null | head -n 1)
          
          if [ -z "$APP_DIR" ]; then
            echo "Error: Could not find the app directory containing docker-compose.yml"
            exit 1
          fi
          
          echo "Found app in: $APP_DIR"
          cd "$APP_DIR"
          
          # Pull the latest changes from GitHub
          git reset --hard
          git pull origin main
          
          # Rebuild and restart the containers with 10 workers
          docker compose up -d --build --scale worker=10
